import {} from "preact";

import { HiExternalLink } from "react-icons/hi";
import "@/styles/banner.css";

const Banner = () => {
  return (
    <div class="banner">
      <span class="title">Join our Discord server for free trial!</span>
    </div>
  );
};

export default Banner;
